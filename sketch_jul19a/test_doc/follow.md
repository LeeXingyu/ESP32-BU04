经过我们多轮的深度探讨，针对“在 RESTful API 限制下实现 UWB 丝滑跟随”这一核心需求，我们构建了一套从底层算法到上层调度的完整架构。以下是整个方案的系统性总结：
一、 核心痛点与破局思路
•痛点：RESTful API 属于“任务级”接口（MoveToAction），每次执行都需经历“全局规划-启动-巡航-停止”的完整周期。如果目标实时移动，频繁下发新任务会导致底盘不断“急刹-重启”，产生严重的顿挫感；且底层导航模式不适合高频动态追踪。
•破局思路：采用**“宏观导航 + 微观相对插点”的双模切换机制**，并在 ESP32 端引入**卡尔曼滤波（Kalman Filter）**进行轨迹预测，将 UWB 的“死坐标”转化为“带速度和方向的动态航路点”。
二、 系统架构设计
系统分为**上位机（ESP32 边缘计算节点）与下位机（思岚底盘）**两部分：
•ESP32 (Arduino IDE)：作为“大脑”，负责高频读取 UWB 串口数据、执行卡尔曼滤波、计算安全距离与预测坐标，并通过 HTTP 请求向底盘下发指令。同时内置轻量级 Web 服务器，接收主机的启停与 TAG 切换指令。
•思岚底盘：作为“四肢”，仅负责执行 `MoveToAction`（导航模式）或 `RotateByAction`（原地旋转），并开放 RESTful API 供 ESP32 调用。
三、 核心算法与控制策略
1. 轨迹预测与平滑（卡尔曼滤波）
•在 ESP32 中实现轻量级 2D 卡尔曼滤波器，过滤 UWB 的多径效应跳变噪声。
•利用滤波后的平滑速度矢量，引入 `Lookahead Time`（如向前预测 0.5~1 秒），计算出“虚拟目标点”，让底盘提前去“截胡”移动中的人，从根本上解决导航延迟导致的追尾问题。
2. 动态双模切换机制
•远距离（>2米）- 全局导航模式：直接将预测后的安全目标点下发给底盘，充分利用底盘自带的 SLAM 算法进行全局路径规划和绕障。
•近距离（≤2米）- 相对插点模式：取消全局规划，ESP32 每隔 0.2~0.5 秒，计算底盘前方 0.5 米的中间点下发给底盘。通过“高频微步”欺骗底盘，模拟出类似底层速度控制的丝滑跟随效果。
3. 安全与姿态控制
•动态安全距离：移动中预留 0.8 米刹车距离，到达后逼近至 0.5 米。
•Yaw 动态锁定：每次下发目标点时，利用 `atan2` 计算底盘指向目标的偏航角，确保底盘在移动和停止时车头始终正对目标。
4. 盲区处理与状态机
•3组历史队列（FIFO）：应对 BU04 的 120° 视场角盲区。信号丢失时不丢弃数据，依靠队列中的历史轨迹继续“盲走”。
•慢速旋转找信号：当信号丢失超过设定阈值（如 2 秒），触发 `RotateByAction` 原地慢速旋转。
•捕获延迟行动：旋转中扫到信号后，不立即猛冲，而是延迟 1.5 秒等待数据稳定后再恢复跟随。
5. 抢占式打断机制（Preemptive Control）
•在 ESP32 中高频监控底盘状态。当新旧目标点距离偏差超过阈值时，主动发送 `DELETE /actions/current` 打断旧任务，无缝下发新任务，避免底盘走回头路。
四、 工程化与调试接口
•Web 控制台：ESP32 开启 8080 端口，支持通过 HTTP GET 请求动态控制底盘。
◦`/start_follow`：启动跟随
◦`/stop_follow`：停止跟随
◦`/set_tag?tag_id=xxx`：运行时动态切换追踪的 UWB TAG ID
•多线程安全：使用 `threading.Lock` 保护串口读取、主控制循环与 Web 服务之间的共享变量，防止数据竞争。
五、 方案总结
这套方案在完全不修改底盘底层 SDK、仅依赖 RESTful API 的严苛限制下，通过**“空间换时间”（相对插点）和“算法换体验”**（卡尔曼预测 + 状态机兜底）的工程手段，成功将原本僵硬的“离散点巡航”优化为了具备商用级体验的“丝滑动态跟随”。整个系统兼顾了全局避障的安全性、近距离跟随的平滑度以及复杂环境下的抗干扰能力。
(AI生成)